/// <reference types="node" />
import { GuildMember } from 'discord.js';
import { CanvasRenderingContext2D as ctx2D, Canvas, Image } from 'canvas';
import { Theme } from '@discord-card/core';
export * from '@discord-card/core';
export declare type ThemeType = (keyof typeof themes) | Theme;
export declare var themes: {
    dark: {
        color: string;
        image: string;
    };
    sakura: {
        color: string;
        image: string;
    };
    blue: {
        color: string;
        image: string;
    };
    bamboo: {
        color: string;
        image: string;
    };
    desert: {
        color: string;
        image: string;
        font: string;
    };
    code: {
        color: string;
        image: string;
        font: string;
    };
};
export declare type ModuleFunction = (ctx: ctx2D) => any;
export declare type CardOptions = {
    theme?: ThemeType;
    title?: string;
    text?: string;
    subtitle?: string;
    avatar?: Canvas | Image | Buffer | string;
    blur?: boolean | number;
    border?: boolean;
    rounded?: boolean;
    custom?: ModuleFunction;
};
export declare function drawCard(options: CardOptions): Promise<Buffer>;
export declare function welcomeImage(member: GuildMember, opts?: CardOptions): Promise<Buffer>;
export declare function goodbyeImage(member: GuildMember, opts?: CardOptions): Promise<Buffer>;
//# sourceMappingURL=index.d.ts.map